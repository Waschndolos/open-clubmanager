node_modules/
server/dist/
# Local Go test binary produced by `go build ./cmd/server`
server/server
server/server.exe

# Tauri
src-tauri/target/
src-tauri/gen/
# Sidecar binaries are built locally; only the .gitkeep placeholder is tracked
src-tauri/binaries/open-clubmanager-server-*